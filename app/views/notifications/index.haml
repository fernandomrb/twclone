.container
    .notifications
        - if @notifications.any?
            - @notifications.each do |notification|
                .notification
                    %p #{notification.actor.username} #{notification.action}
        - else
            %h2 No tienes notificaciones