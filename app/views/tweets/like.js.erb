$("#tweet_<%= @tweet.id %> .like a").attr('href', '<%= dislike_tweet_path(@tweet) %>')
																		.addClass('liked')
																		.children('.icon-container')
																		.children('.fa-heart')
																		.attr('data-original-title', 'Undo like')
																		.parent('.icon-container')
																		.siblings('.text-container')
																		.children('.action-count')
																		.children('span')
																		.text("<%= @tweet.get_likes.size %>");

<% if !@retweet.nil? %>																		
	$("#tweet_<%= @retweet.id %> .like a").attr('href', '<%= dislike_tweet_path(@tweet) %>')
																			.addClass('liked')
																			.children('.icon-container')
																			.children('.fa-heart')
																			.attr('data-original-title', 'Undo like')
																			.parent('.icon-container')
																			.siblings('.text-container')
																			.children('.action-count')
																			.children('span')
																			.text("<%= @tweet.get_likes.size %>");
<% end %>